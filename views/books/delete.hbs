<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-body text-center">
                <h3 class="card-title text-danger">
                    <i class="bi bi-exclamation-triangle"></i> Confirmar Eliminación
                </h3>
                
                <div class="mb-3">
                    <img src="{{book.coverImage}}" alt="{{book.title}}" 
                         class="img-thumbnail" style="width: 150px; height: 200px; object-fit: contain;">
                </div>
                
                <p class="card-text">
                    ¿Está seguro que desea eliminar este libro?
                </p>
                
                <div class="alert alert-info">
                    <strong>{{book.title}}</strong><br>
                    <small class="text-muted">
                        Año: {{book.publicationYear}}
                    </small><br>
                    <small class="text-muted">
                        Categoría: {{getCategoryName categoriesList book.categoryId}}
                    </small><br>
                    <small class="text-muted">
                        Autor: {{getAuthorName authorsList book.authorId}}
                    </small><br>
                    <small class="text-muted">
                        Editorial: {{getPublisherName publishersList book.publisherId}}
                    </small>
                </div>

                <form method="POST" action="/books/delete" class="d-inline">
                    <input type="hidden" name="BookId" value="{{book._id}}">
                    <a href="/books" class="btn btn-secondary me-2">
                        <i class="bi bi-x-circle"></i> Cancelar
                    </a>
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Eliminar
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>